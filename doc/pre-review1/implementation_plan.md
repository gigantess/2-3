# 📋 AI 사전평가(pre-review1) 100% 달성을 위한 개선 구현 계획서

`doc/pre-review1/review_result.md`에 작성된 사전평가 결과(19개 항목 중 13개 통과, 6개 미통과)를 분석하여, 미통과 항목 6개(FAIL)를 완전히 해결하고 통과 항목(PASS)의 13개 보완 제안까지 모두 반영하기 위한 세부 구현 계획입니다.

---

## 🎯 개요 및 목표

- **평가 결과**: 68% (13 / 19 항목 통과) -> **100% (19 / 19 항목 통과)** 목표
- **주요 해결 과제**:
  1. **FAIL 6개 항목 완전 해결**: 프론트/백엔드 분리 이유, 배포 로그 진단 가이드, HTML/CSS/JS 기술 역할, fetch 비동기 흐름도, 지연(Latency) 완화 전략, 프레임워크 도입 영향도 분석 표 추가
  2. **PASS 13개 항목 보완 제안 반영**: `.env.example` 샘플 파일 추가, API 요청/응답 JSON 스키마 명시, 프롬프트 설계 의도, 키 유출 대응 프로세스, 검증 페이지 목록 및 스크린샷 인덱스 제공

---

## 🔍 개선 항목별 세부 계획

### 1. 🚨 미통과(FAIL) 항목 개선 계획 (6개)

| 평가 항목 | 부족했던 점 | 개선 및 보완 계획 |
|---|---|---|
| **#8 (구조 분리 사유)** | 프론트/백엔드 구조 분리의 선택 사유 설명 누락 | README에 보안(API 키 노출 방지), 배포 독립성(Serverless Function), 코드 유지보수성 관점의 분리 이유 추가 |
| **#11 (배포 문제 진단)** | 배포 모니터링 및 로깅/재배포 절차 문서화 누락 | Vercel Function Logs, 브라우저 개발자 도구(Console/Network) 활용 방법 및 Vercel CLI/GitHub push 기반 재배포 흐름 작성 |
| **#12 (기술 역할 명시)** | HTML/CSS/JS의 역할에 대한 설명 누락 | HTML5(시맨틱 구조), CSS3(B급 키치 테마, 반응형, 다크모드), JS(DOM 조작, 이벤트 핸들링, 비동기 fetch) 역할 명시 |
| **#13 (fetch 데이터 흐름)** | 클라이언트-서버리스 비동기 호출 흐름 설명 누락 | `사용자 입력 폼` ➔ `JS fetch('/api/generate')` ➔ `Vercel Python Function` ➔ `AI API 호출` ➔ `JSON 응답` ➔ `DOM 렌더링` 전체 흐름 서술 |
| **#16 (지연 완화 전략)** | 지연(Latency) 및 비용/쿼터 개선 방안 누락 | AbortController 타임아웃, Gemini Flash 고속 모델 채택, 프롬프트 압축, 캐싱(LocalStorage) 및 비동기 처리 전략 기술 |
| **#19 (프레임워크 분석)** | 프레임워크 도입 시 장단점 및 변경 범위 비교 누락 | React/Next.js 전환 시 기대 이점(상태 관리, 컴포넌트 재사용) 대 변경 범위(JSX 리팩토링, 빌드 체인 도입) 비교 표 추가 |

---

### 2. 💡 PASS 항목 보완 제안 반영 계획 (13개)

1. **#1 (검증 페이지 목록)**: 배포 URL 하단에 `대문(Hero)`, `명언기(Generator)`, `갤러리(Gallery)`, `보관함(Archive)` 4개 검증 섹션 요약
2. **#2 (반응형 브레이크포인트)**: `320px(모바일 소형)`, `768px(태블릿)`, `1024px+(데스크톱)` 반응형 레이아웃 검증 요약
3. **#3, #10 (JSON 요청/응답 샘플)**: `/api/generate` 및 `/api/discord` 엔드포인트의 입력/출력 JSON 샘플 추가
4. **#4 (실제 입출력 테스트 사례)**: 정상 입력, 빈 입력, 긴 입력에 대한 실제 API 처리 테스트 사례 수록
5. **#5, #9 (상태 관리 및 오류 처리 UX)**: 로딩 ➔ 성공 ➔ 실패 상태 전환 및 재시도 UX 설명 추가
6. **#6 (로컬 환경 설정)**: `.env.example` 샘플 파일 작성 및 `.env` 등록 방법 문서화
7. **#7 (제출 패키지 파일 위치)**: `doc/SERVICE_PLAN.md`, `screenshot/` 등의 파일 경로 구조표 추가
8. **#14 (운영 환경 변수 관리)**: Production / Preview / Development 환경 변수 격리 관리 팁 기술
9. **#15 (프롬프트 설계 의도)**: B급 팩트폭행 페르소나 및 80자 이내 규칙 등 프롬프트 엔지니어링 핵심 정리
10. **#17 (확장 설계)**: 추가 API 연동(네이버 검색, 카카오톡 공유 등) 및 프론트 위젯 확장 가능성 서술
11. **#18 (키 유출 시 대응 절차)**: API 키 유출 감지 시 즉시 폐기/재발급 절차 및 Git history 삭제(`git filter-repo` 등) 가이드 포함

---

## 🛠️ 변경 대상 파일

### [NEW] [.env.example](file:///d:/cody/2-3/.env.example)
- 로컬 개발 및 테스트를 위한 환경 변수 키 샘플 파일 생성 (`GEMINI_API_KEY`, `DISCORD_WEBHOOK_URL` 구조)

### [MODIFY] [README.md](file:///d:/cody/2-3/README.md)
- 위의 모든 FAIL 보완 항목 6개와 PASS 보완 항목 13개를 체계적인 마크다운 섹션으로 통합 업데이트

---

## 🧪 검증 계획

1. **문서 검토 및 링크 검증**: README.md의 상대 경로 링크 및 마크다운 표, 코드 블록 렌더링 확인
2. **환경변수 샘플 검증**: `.env.example` 파일 구성 정상 여부 확인
3. **체크리스트 재점검**: `doc/pre-review1/review_result.md` 19개 항목과 1:1 대조하여 누락 항목 없는지 확인
